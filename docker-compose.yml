version: "3.3"
services:
  media-server:
    container_name: media-server
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - media-server

networks:
  media-server:
    driver: bridge
    name: media-server
